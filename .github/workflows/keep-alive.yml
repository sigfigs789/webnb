name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 12 */3 * *'  # every 5 days at noon UTC
  workflow_dispatch:         # allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -sS -f \
            -H "apikey: sb_publishable_SThUIT8wLCxYuerYqVC-XQ_YKUEAn1a" \
            "https://rhgtdlizspaitfrepogy.supabase.co/rest/v1/" \
            && echo "Supabase is alive"
